# AGENTS.md — このリポジトリで作業する人・AIエージェントへのルール

作業を始める前に読んでください。背景と経緯は [docs/LESSONS_LEARNED.md](docs/LESSONS_LEARNED.md) にあります。

## 1. データと個人情報
- **このリポジトリは公開（public）です。** 申告書の数字、物件名・所在地、氏名などの個人情報を、コード・テスト・ドキュメント・コミットメッセージに書かない。
- 開発用DB（`tax-filing-app`）には **実際の申告データ** が入っている前提で扱う。
- DBを変更・削除する操作は、次の順で行う。
  1. dry-run で対象を表示する
  2. 対象を `backend/backups/` にバックアップする（gitの管理外）
  3. 利用者の明示的な承認を得る
  4. 実行したら、結果を確かめる
- 既存のデータに取り込む前に、**すでに登録されているデータを確認する**（重複や上書きを防ぐ）。

## 2. 税額計算
- 法定の数値（控除額、税率、端数処理）は `backend/src/services/taxRules.ts` だけに置く。ほかのファイルに直接書かない。
- 数値は **国税庁タックスアンサーなどの一次情報** から取る。記憶や推測で書かない。出典と「何年何月現在の法令か」をコメントに残す。
- 未公表の年分は、公表済みの最新ルールで計算し、`isProvisional` と `notice` を付ける。黙って推測値を使わない。
- 金額は整数で計算する（`a * 2.8` ではなく `(a * 28) / 10`）。区分の境界は「以上・未満」を国税庁の表のとおりにする。
- 年分によって結果が変わる値は、結果に `taxYear` を持たせる。画面で年度を切り替えたら結果を破棄する。

## 3. テスト
- **修正を外すと失敗することを確かめてから、テストを完成とする。**
- 次の書き方は禁止（常に合格してしまう）。
  - `expect(x || true)`
  - 件数に対する `toBeGreaterThanOrEqual(0)`
  - `if (await el.isVisible()) { ... }` の中にしか検証がない
  - `.catch(() => ...)` で失敗を握りつぶす
- モックは実装の呼び出し方に合わせる（例: `find().sort().lean()` はチェーンとしてモックする）。実装にない関数をテストしない。
- E2E は **E2E専用のDB（`tax-filing-app-e2e`）** で実行する。実在の物件IDやユーザーデータを使わない。テスト用のデータは `backend/scripts/seed-e2e.ts` で作る。
- E2E を実行するときは、開発用のバックエンドを止める（動いていると、安全のため起動に失敗する）。

## 4. 実装上の落とし穴
- **Mongoose のドキュメントを `{...doc}` で展開しない**。値が失われる。`.lean()` で取得するか、`doc.toObject()` を使う。
- エラーを `catch` して空の値（`[]` や 0）を返さない。呼び出し元に伝える。
- 一意インデックスは、既存のデータに重複があると **黙って作られない**。モデルの `index` イベントで失敗を検知し、`npm run db:dedupe-salary` で整理する。
- 更新（`findByIdAndUpdate`）で enum などを検証させるには `runValidators: true` が必要。
- Express 5 のルートでは、`/:year(\d{4})` のような正規表現つきのパスは使えない。形式はハンドラの中で確かめる。

- 共通のフォーム部品（`Input`・`Select`）はラベルと入力欄を `useId` でつないでいる。新しい部品でも `<label htmlFor>` と `id` を必ずつなぐ。
- バックエンドの `npm run dev` は変更を自動で読み込まない。変更したら再起動する。
- アプリは `?module=` をURLから消す。E2E で画面を開き直すときは reload ではなく同じURLへ `goto` する。
- E2E の spec は並行に動く。年度ごとに1件のデータ（給与所得など）は、spec ごとに別の年度を使う。

## 5. 作業の進め方（AIエージェント向け）
- サブエージェントの出力や報告は、**必ず自分でコードとテスト結果を見て確かめる**。
- **ファイルの削除を、そのファイルを使う処理と並行で実行しない**（依存する処理がすべて終わってから削除する）。
- 一時ファイルには `tmp_rovo_` を付け、作業の最後に削除する。個人データを含む一時ファイルは、リポジトリの外に置く。
- git の書き込み（commit、push、PR、merge）は、利用者から依頼されたときだけ行う。
